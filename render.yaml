services:
  - type: web
    name: quick-and-quality-api
    env: node
    rootDir: backend
    buildCommand: |
      corepack enable pnpm
      pnpm install --frozen-lockfile
    startCommand: pnpm start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 20

